# [Название сценария]

## Фасеты

| Фасет | Значение |
|---|---|
| **ID** | `func-NNN` |
| **ai_pattern** | `rag` / `nql` / `classification` / `generation` / `agent` / `extraction` / `monitoring` |
| **function** | `sales` / `hr` / `operations` / `finance` / `it` / `marketing` / `management` / `legal` / `rd` |
| **industry** | `universal` / `manufacturing` / `services` / `retail` / `it-company` / `education` / `healthcare` / `fintech` |
| **autonomy** | `chat` / `cowork` / `agent` / `multi-agent` |
| **process_maturity** | `chaos` / `process-exists` / `data-exists` |
| **effect** | `time-saving`, `error-reduction` *(можно несколько через запятую)* |

## Карточка

| Поле | Значение |
|---|---|
| **scale** | `micro` / `meso` / `macro` |
| **input_type** | `text` / `documents` / `audio` / `structured-data` / `api` / `mixed` |
| **complexity** | `no-code` / `low-code` / `custom-dev` |
| **budget** | `free` / `low` / `medium` / `high` |
| **time_to_result** | `days` / `weeks` / `months` |
| **status** | `verified` / `hypothesis` / `in-progress` |

## Триггер

> Что именно происходит, чтобы запустился этот сценарий?

## Вход

> Что подаётся на вход ИИ? (формат, источник)

## Выход

> Что получается на выходе? (формат, куда попадает)

## Боль

> Какую проблему решает сценарий без ИИ?

## Решение

> Как ИИ решает эту проблему?

## Шаги внедрения

1. **Шаг 1** — описание
2. **Шаг 2** — описание

## Инструменты

| Инструмент | Роль |
|---|---|
| Claude / GPT-4 | LLM-ядро |

## Результаты

| Метрика | До | После |
|---|---|---|
| Время на задачу | X | Y |

## Подводные камни

- Камень 1

## Теги

`тег1`, `тег2`
